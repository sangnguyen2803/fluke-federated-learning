hyperparameters:
  client:
    batch_size: 256
    local_epochs: 10
    loss: CrossEntropyLoss
    optimizer:
      name: AdamW
      lr: 1e-3
      weight_decay: 1e-4
    scheduler:
      gamma: 1
      step_size: 1
  model: MNIST_2NN
  server:
    global_step: 2
name: fluke.algorithms.scaffold.SCAFFOLD
